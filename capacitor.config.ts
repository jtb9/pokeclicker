import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.simulearn.barnyak.pokeclicker',
  appName: 'pokeclicker',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
